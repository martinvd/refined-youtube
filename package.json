{
  "name": "refined-youtube",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch scripts/build.ts",
    "build": "tsc && bun run scripts/build.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.13",
    "@types/chrome": "^0.1.40",
    "typescript": "~6.0.2"
  }
}
